1. Run BinWaves in each grid indivudually (1. Propagation and Whacs buoy correction. In some grids superPoint was used due to the shape of the region. teh rets of teh file are optional validations, the Jan97 files was beacsue we noticed we did´t procces taht month so we have to do all over again only for that month, since was a problem in teh WHACS data download for that month). Here and extra step of averaging Kp was done due to teh computational cost of teh next step when reconstructingteh spectra (too many frequencies and directions), however a new code was implementeed whih is way faster thatn teh opriginal code,  threfore this steop wont be needed. 
utils/reconstruct spectra (This is a faster code, no using DASk as the original version), the final reconstr5ucted spectra were saved in (xxxx) due to teh size of th files
Partitions (create partitions of all variables ofr each grid )